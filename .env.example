# Garak Security Testing Frontend Environment Variables
# Copy this file to .env.local and update the values for your environment

# Auth Service URL - Handles authentication (login, register)
# This service provides admin login and registration endpoints
NEXT_PUBLIC_AUTH_URL=http://localhost:3001

# Management Service URL - Handles business logic (requires Bearer token)
# This service provides detector, category, and probe management endpoints
NEXT_PUBLIC_MANAGEMENT_URL=http://localhost:3002

# API Base URL - Fallback for other services (jobs, tests, etc.)
# Default: http://localhost (for local development)
# Production: Update this to your production API server URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Example production values:
# NEXT_PUBLIC_AUTH_URL=https://auth.yourdomain.com
# NEXT_PUBLIC_MANAGEMENT_URL=https://management.yourdomain.com
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
